import 'package:flutter/cupertino.dart';

class OtpIsNotRequired extends StatelessWidget {
  const OtpIsNotRequired({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      width: 100,
      child: Text('Otp is not required'),
    );
  }
}
